// main.js or similar file is gonnbe where all of the website logic going to sit

// Util.js file can be stored somewhere else, like a web server, so anytime you want to use your function or library you can use it

import { add, anotherFunction } from "./Util";

console.log(add([1, 2, 3]));